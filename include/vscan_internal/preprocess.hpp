#pragma once
#include "vscan_internal/frame.hpp"

namespace vscan {

// Frame -> GrayView 변환.
// - format == GREY: ISP가 이미 그레이스케일을 준 경우. 복사 없이 frame.data를
//   그대로 가리키는 뷰만 반환한다 (진짜 zero-copy 경로, 이게 기본 경로여야 함).
// - format == YUYV/NV12: ISP 설정이 잘못됐을 때의 폴백. 이 경우엔 GrayImage로
//   실제 변환(복사 1회, NEON 가속)이 발생하고, 반환된 GrayView는 그 GrayImage를
//   가리킨다 — 그래서 GrayImage& storage 파라미터로 수명을 보장받아야 한다.
GrayView toGrayView(const Frame& frame, GrayImage& fallbackStorage);

// NxN 박스 평균으로 1/N 해상도 사본을 만든다 (N = 2 또는 3).
// 용도: 2단계 디코드의 "coarse locate" — 이진화가 locate 비용의 대부분이고
// 픽셀 수에 비례하므로 1/N이면 1/N^2이 된다.
// 실측(악조건 36종, locate 합계): 원본 283ms / 1/2 93ms / 1/3 59ms.
// 1/3이 1/2와 검출 개수가 같으면서 1.6배 빨라서 기본값이다.
// 나머지 픽셀은 버림(마지막 행/열 무시) — locate 용도라 무해하다.
// 자동 벡터화가 걸리는 형태로 작성돼 있다(A53 NEON 확인).
void downsampleBox(const GrayView& src, GrayImage& dst, int factor);

// [1D 바코드 회전 구제용] 프레임(또는 crop)을 임의 각도로 회전한다.
// DDA 방식(행마다 삼각함수 1회만 계산, 나머지는 증분)이라 픽셀당
// 삼각함수를 다시 부르는 나이브한 구현보다 5~6배 빠르다(실측).
// 최근접 이웃 샘플링(품질보다 속도 우선 — 이 용도는 회전 후 바로
// zxing이 재이진화하므로 약간의 앨리어싱은 무해하다).
// out은 src와 동일 크기(중심 기준 회전, 프레임 밖으로 나간 부분은
// 흰색(255)으로 채움 — zxing 배경색과 동일해 안전).
void rotateAroundPoint(const GrayView& src, float degrees, float pivotX, float pivotY, GrayImage& out);

// [DPM/점각인 구제용] "어두운 점들을 연속된 면으로 합치기".
//
// DPM(레이저 점각인) 코드는 모듈 하나가 통짜 면이 아니라 작은 점
// 하나라서, zxing의 8x8 블록 이진화가 지역 평균을 내면 흰 배경과
// 검은 점이 섞여 애매한 회색으로 뭉개진다 — 파인더 패턴(모서리
// 정사각형)조차 점 격자라 위치 탐색 첫 단계부터 막힌다.
//
// 실측(실물 비교 대상 리더기 대조 검증까지 완료): 반전 -> 닫힘(팽창 후 침식) ->
// 재반전 하나로 해결된다. "닫힘"이 아니라 "반전 후 닫힘"인 이유 —
// 그레이스케일 팽창은 밝은 영역을 키우는 연산(이웃 중 최댓값)인데,
// 우리가 키우고 싶은 건 어두운 점이므로 먼저 반전해서 점을 밝게
// 만든 다음 팽창해야 한다.
//
// 구현은 커널 크기와 무관하게 O(1)/픽셀인 monotonic deque 기반
// 슬라이딩 최댓값/최솟값(가로 패스 + 세로 패스로 분리) — 나이브한
// O(k^2)/픽셀 대비 커널이 커질수록 이득이 크다.
//
// kernelSize: 점 사이 간격보다 살짝 크게. 실측(점 간격 5px 기준)
// k=5~9 전부 성공, 커널이 클수록 비용은 늘고 두꺼운 선이 뭉개질
// 위험도 커지므로 기본값을 5로 둔다(가장 작은 값도 성공했으므로
// 부작용 최소화 우선).
void morphologicalCloseInverted(const GrayView& src, int kernelSize, GrayImage& out);

/*
 * 3x3 박스 블러 (가로/세로 분리형).
 *
 * 용도는 하나다: **노이즈 구제 단계**. 센서 노이즈가 심하면 이진화가 무너져
 * 코드가 통째로 안 읽히는데, 모듈 크기가 노이즈 상관거리보다 크면 살짝만
 * 뭉개도 신호가 살아난다. 실측(모듈 4px, 노이즈 시그마 20~60 스윕):
 * Code128 검출 23.8% -> 100%, QR 76.2% -> 100%.
 *
 * 분리형이라 픽셀당 덧셈 4회 수준이고, 자동 벡터화가 잘 걸리는 형태로
 * 썼다(누적 변수 없이 인접 3픽셀 합 — [[vscan-lite-deskew-neon]]과 같은 이유).
 */
void boxBlur3x3(const GrayView& src, GrayImage& out);

/*
 * [반경 지정 박스 블러] 분리형 이동합이라 반경과 무관하게 화소당 상수
 * 시간이다. radius=1이면 boxBlur3x3과 같다.
 *
 * 노이즈 구제의 2단에서 쓴다. 3x3을 여러 번 겹치는 것보다 낫다 — 같은
 * 세기를 내는 데 필터 통과가 한 번뿐이다(3x3 네 번 ≈ 반경 2 한 번).
 */
void boxBlur(const GrayView& src, int radius, GrayImage& out);

/*
 * [조명 평탄화] 큰 반경으로 뭉갠 판본을 "배경 밝기"로 보고 나눈다.
 * out = clamp(128 * src / blur(src, radius)).
 *
 * 그림자가 코드를 가로질러 지나가면 한 프레임 안에서 밝기가 두 배 넘게
 * 차이 난다. 그러면 (a) 에너지 로케이터가 어두운 쪽을 코드로 안 보고
 * (b) 크롭 하나에 임계 하나를 쓰는 모든 전처리가 한쪽을 통째로 검게
 * 만든다. 실측(Code128 module 8, 그림자 축 — 코드 중앙에서 오른쪽 절반이
 * 어두워진다): 0.2/0.3에서는 상자가 아예 왼쪽 절반(465x341)만 잡혔다.
 * 코드가 972px인데 그 절반만으로는 어떤 구제도 못 읽는다.
 *
 * 나누고 나면 배경이 평평해지고 막대 구조만 남는다 — 실측: 그림자
 * 0.2~0.5 네 단이 전부 **평탄화 뒤 평범한 풀프레임 디코드**로 열린다.
 * 반경은 64~128 어디서나 같고(32는 0.2에서 아슬아슬, 192는 0.2를 놓친다),
 * 코드 폭보다 충분히 커야 막대 자신이 배경으로 흡수되지 않는다.
 *
 * 배경이 이미 평평하면 false를 돌려준다(뭉갠 판본의 상하위 백분위수
 * 차이가 minSpread 미만). 그 경우 out은 건드리지 않는다.
 */
bool flattenIllumination(const GrayView& src, int radius, GrayImage& out, int minSpread = 40);

/*
 * [세로 이동평균] 열마다 위아래 radius 픽셀을 평균한다. 가로 방향은
 * 손대지 않는다.
 *
 * 1D 바코드는 정보가 **가로에만** 있고 세로로는 같은 값이 반복된다.
 * 그래서 세로로만 평균하면 잡음 시그마가 sqrt(2r+1)배 줄어드는데 막대
 * 경계는 그대로다 — 등방 블러처럼 얇은 막대를 뭉개지 않는다.
 *
 * 저대비 구제에서 필요했다. 실측(Code39 module 8, 대비 0.05): 여백을
 * 걷은 크롭에 스트레칭 + 국소 이진화를 걸어도 안 읽히는데, 그 앞에
 * 세로 평균(반경 2 이상)을 한 번 넣으면 읽힌다. 반경 0에서는 안 된다.
 *
 * 기울어진 코드에는 맞지 않는다(막대 방향이 세로가 아니다). 사다리
 * 끝의 추가 판본으로만 쓸 것.
 */
void verticalBlur(const GrayView& src, int radius, GrayImage& out);

/*
 * [행 단위 이진화 — 1D 전용]
 *
 * 행마다 **그 행 자신의** 백분위수(pct / 100-pct)를 재서 중간값으로 가른다.
 * 구조가 없는 행(범위가 minRange 미만)은 흰색으로 민다.
 *
 * 왜 블록이 아니라 행인가. 1D 코드는 한 행이 코드 전체를 담고 있고,
 * 그 행 안에서 밝은 요소와 어두운 요소가 반드시 둘 다 나온다. 그래서
 * 행 하나의 히스토그램이 그 행의 임계를 정하는 데 필요한 정보를 다 갖는다.
 * 블록 단위(localAdaptiveBinarize)는 블록이 굵은 요소 안에 통째로 들어가는
 * 경우를 이웃에서 빌려 메워야 하는데, 대비가 아주 낮으면 그 추정이 무너진다.
 *
 * 실측(module 8, 대비 0.05 — 다른 모든 전처리가 실패하는 구간): 코드 행의
 * 런 개수는 대비 1.0일 때와 **정확히 같다**(Codabar 81, Code93 99, ITF 49,
 * UPC-E 35). 진폭만 19계조로 줄었을 뿐 정보는 그대로다. 행 단위 임계는
 * 그 19계조 안에서 정확히 갈라서 일곱 종(Codabar/Code39/Code93/DataBar/
 * ITF/PDF417/UPC-E)을 전부 연다. 같은 이미지에 블록 이진화를 걸면 넷이
 * 안 열린다.
 *
 * 세로 평균을 앞에 넣어야 한다(verticalBlur 반경 1이면 충분). 잡음이
 * 행 백분위수를 흔들기 때문이다.
 *
 * 막대가 세로일 때만 맞는 가정이다 — 기울어진 코드나 2D에는 쓰지 말 것.
 * 반환값 false는 "구조가 있는 행이 너무 적다"는 뜻이다.
 */
bool rowBinarize(const GrayView& src, GrayImage& out, int pct = 10, int minRange = 10);

/*
 * [프레임 노이즈 추정 — 전처리를 고르기 위한 사전 측정]
 *
 * 왜 필요한가. 지금 파이프라인은 어떤 프레임이든 풀프레임 두 번(coarse +
 * fast, 13ms)으로 시작하고, 실패하면 TryHarder / +Invert / 노이즈 구제 /
 * DPM 구제를 **전부 풀프레임에** 건다. 노이즈가 심한 프레임에서는 그
 * 전부가 실패하므로 시간만 쓴다 — 실측(Code128 module 8, 노이즈 시그마 40):
 * zxing 호출 16회, 652ms.
 *
 * 그런데 그 프레임에 **3x3 블러를 먼저 한 번** 먹이고 넣으면 호출 2회에
 * 24.1ms로 끝난다(fast 패스에서 바로 붙는다). **35배**다. 없는 기능을
 * 만드는 게 아니라 이미 있는 단계(노이즈 구제)를 앞으로 옮기는 것이고,
 * 옮겨도 되는지는 이 함수가 판정한다.
 *
 * 방법은 Immerkær의 라플라시안 추정이다. 라플라시안 마스크는 평탄면과
 * 완만한 기울기를 0으로 만들고 화소 단위 요동만 남기므로, 그 응답의 크기가
 * 곧 노이즈 진폭이다. 다만 코드의 모듈 경계도 라플라시안에 크게 잡히므로
 * **평균이 아니라 하위 백분위수**를 쓴다 — 프레임의 대부분은 배경이라,
 * 하위 25%는 배경(=노이즈)만 본다.
 *
 * 실측(2048x1536 합성 + 실물 캡처):
 *
 *   프레임              p25    p50
 *   깨끗                3.5    6.0
 *   모듈 2px            4.0    6.0
 *   블러 2.0             —     5.5
 *   회전 45도            —     5.5
 *   대비 0.10            —     6.0
 *   실물 3.1MP 차트     0.0    0.0
 *   노이즈 시그마 20   20.0   33.0
 *   노이즈 시그마 40   37.5   61.5
 *   노이즈 시그마 60     —    83.0
 *
 * p25 기준 임계 12면 5배 여유로 갈린다. **실물 카메라 캡처가 0.0인 것이
 * 중요하다** — 진짜 캡처를 헛되이 뭉개지 않는다는 뜻이다.
 *
 * rowStep/colStep: 표본 간격. 노이즈는 화소 단위 통계라 표본이 수만 개면
 * 백분위수가 안정적이다. 실측(2048x1536, 깨끗/노이즈40 두 프레임):
 * 4x2부터 16x8까지 **추정값이 소수점까지 완전히 같고** 비용만 0.76ms ->
 * 0.046ms로 줄었다. 기본값 8x4(표본 약 10만개, 0.19ms)는 그 사이에서
 * 여유를 둔 값이다.
 * [[vscan-lite-noise-estimate]]
 */
float estimateNoise(const GrayView& src, int rowStep = 8, int colStep = 4);

/*
 * [프레임 국소 명암 폭 추정 — "이 프레임은 지금 상태로 읽힐까"]
 *
 * 8x8 블록의 (최대-최소) 중 상위 1% 값을 돌려준다. 즉 "프레임에서 가장
 * 대비가 센 자리가 얼마나 센가"다.
 *
 * 왜 이 형태인가. 목적은 저대비 프레임을 가려내는 것인데, 프레임 전체의
 * 계조 폭(p1~p99)을 쓰면 **코드가 작을 때 배경만 보게 된다** — 실측:
 * 모듈 2px 프레임은 대비가 멀쩡한데도 폭이 31로 나왔다(코드가 프레임의
 * 1% 미만이라 백분위수가 전부 배경이다). 블록 범위의 상위값은 코드가
 * 아무리 작아도 그 자리를 집어낸다.
 *
 * 실측(2048x1536):
 *
 *   프레임            p1~p99 폭   블록범위 p99
 *   대비 0.05            102            91
 *   대비 0.10            108            85
 *   대비 0.20            121            72
 *   대비 0.30            134            90
 *   대비 0.40            147           115
 *   모듈 2px              31           255   <- 폭은 실패, 블록범위는 성공
 *   깨끗 / 회전 / 노이즈  255           255
 *   블러 2.0             247           254
 *   실물 3.1MP 차트      228           255
 *
 * zxing의 이진화가 8x8 블록의 명암 폭 24를 기준으로 "구조 없음"을
 * 판정하므로(preprocess.hpp localAdaptiveBinarize 주석), 이 값은 그
 * 판정과 같은 눈금 위에 있다.
 *
 * 표본 16x8에서 2048x1536 기준 0.10ms다(8x4의 0.41ms와 값이 완전히 같다).
 * [[vscan-lite-contrast-estimate]]
 */
int estimateLocalRange(const GrayView& src, int rowStep = 16, int colStep = 8);

/*
 * 퍼센타일 기반 선형 대비 스트레칭. 하위/상위 0.5%를 잘라 0~255로 편다.
 * 이미 계조를 거의 다 쓰고 있으면(유효 폭이 minSpan 이상) 아무것도 하지
 * 않고 false를 돌려준다 — 호출부가 헛디코드를 건너뛸 수 있게.
 *
 * minSpan 기본 200의 근거: 이 함수가 하는 일은 [lo,hi]를 [0,255]로 펴는
 * 것뿐이라, lo/hi가 이미 양끝에 가까우면 LUT가 거의 항등식이 되어 결과가
 * 안 바뀐다. 처음에 110으로 잡았다가 **정작 살리려던 케이스가 전부
 * 걸러졌다** — 배경이 밝고 코드만 저대비인 프레임은 (배경 - 가장 어두운
 * 바)가 이미 128쯤 되기 때문이다. 코드 자체의 대비와 프레임 전체의
 * 계조 폭은 다른 값이다.
 *
 * 용도는 **저대비 ROI 구제**다. 실측(모듈 6px, 대비 축 스윕): 심볼로지마다
 * 끊기는 대비가 다른데 EAN/UPC 계열만 유독 높다.
 *   Code128  0.15까지 읽힘
 *   QR       0.20까지
 *   EAN13/EAN8/UPCA  0.30까지   <- 두 배가 필요하다
 * 저대비 프레임에서 코드 영역만 잘라 이걸 먹이면 EAN13/UPCA가 0.20까지
 * 내려간다(0.20/0.25 각각 실패 -> 성공).
 *
 * [중요 — 반드시 ROI 안에서만 할 것] 프레임 전체에 걸면 오히려 깨진다.
 * 실측: 전역 스트레칭은 EAN/UPC는 똑같이 살리면서 QR 0.20/0.25/0.30을
 * 전부 죽였다(원본에서는 읽히던 것들이다). 프레임 전체의 최소/최대는
 * 배경과 조명이 지배하는데, 그 값으로 만든 LUT는 코드 영역 안의 계조를
 * 오히려 뭉갠다. ROI 안의 히스토그램으로 만들면 그런 일이 없다.
 * [[vscan-lite-roi-contrast-stretch]]
 */
/*
 * ROI의 계조를 0~255로 편다. 이미 충분히 쓰고 있으면(span >= minSpan) false.
 *
 * `measureNum/measureDen`은 **범위를 재는 창**을 안쪽으로 좁힌다(기본 1/1 =
 * 전체). 왜 필요한가 — ROI 크롭에는 코드 주변 장면이 같이 들어오는데,
 * 그 장면이 고대비면 lo/hi를 그쪽이 정해버려서 **정작 코드는 하나도 안
 * 펴진다**. 실측(QR module 6, 대비 0.05): 코드 자체의 변조는 13계조인데
 * 크롭 전체 span은 117이라 스트레칭이 사실상 항등이 되고 미검출이었다.
 * 안쪽 60%에서만 재면 같은 크롭이 읽힌다.
 */
bool stretchContrast(const GrayView& src, GrayImage& out, int minSpan = 200,
                     int measureNum = 1, int measureDen = 1);

/*
 * [국소 적응 이진화 — 초저대비 전용]
 *
 * 국소 임계보다 어두우면 검정, 아니면 흰색. 단 "구조가 있는 자리"에서만
 * 그렇게 하고, 나머지는 통째로 흰색으로 민다.
 *
 * 왜 stretchContrast()로 부족한가. 그건 ROI 하나에 LUT 하나를 쓰는
 * **전역** 변환이라, ROI 안에서 밝기 차가 크면 코드가 쓰는 계조 구간이
 * 그 차이에 눌린다. 실측(Code128 module 8, 대비 0.10, 2048x1536):
 * 영역 크롭 1184x832의 퍼센타일이 lo 108 / hi 220이었는데, 정작 코드는
 * 그 안에서 110(바)~140(공백)만 쓴다. span 112 중 코드 몫이 30뿐이라,
 * 0~255로 펴봐야 코드는 4~68에 머문다.
 *
 * 그러면 왜 그게 안 읽히나. zxing의 LocalAverage(HybridBinarizer)는
 * 8x8 블록의 (최대-최소)가 **24 이하면 "구조 없는 균일 블록"으로 보고**
 * 평균 대신 min/2를 흑점으로 쓴다 — 노이즈에서 데이터를 만들어내지
 * 않으려는 안전장치다(HybridBinarizer.cpp MIN_DYNAMIC_RANGE). 모듈이
 * 8px면 8x8 블록이 통째로 한 모듈 안에 들어가는 일이 흔한데, 그 블록의
 * 범위는 노이즈뿐이다. 실측(대비 0.10): 코드 영역 8x8 블록의 34%가
 * 24 미만이었다(32x32 블록으로 보면 3%뿐 — 정보는 32px 규모에 있는데
 * 판정은 8px 규모에서 한다).
 *
 * [계조를 손보려던 시도 둘 — 반복하지 말 것]
 *  1. 블록별 (min,max)를 0~255로 펴기. 한 모듈 안에 통째로 들어간 블록을
 *     구제하려고 min/max를 3x3 팽창시켰더니, 코드 가장자리 블록의 max가
 *     **종이 배경**을 끌어와서 코드 자신의 대비가 다시 눌렸다. 실측
 *     (QR module 8, 대비 0.10): 종이 210 / 밝은 모듈 141 / 어두운 모듈
 *     115인데 hi=210으로 펴니 밝은 모듈 69, 어두운 모듈 3 — **둘 다
 *     검정**이 됐다. 전역 스트레칭의 실패가 블록 규모에서 반복된 것이다.
 *  2. 평균 기준 증폭(out = mean + (v-mean)*gain). 1D는 살아났지만
 *     (Code128/EAN13 대비 0.10 통과) 2D는 gain 3~10 x block 24~64
 *     24조합이 전부 실패했다. 대비를 키워도 zxing의 8x8 판정을 통과시키지
 *     못했다.
 *
 * 그래서 계조를 손보는 대신 **판정을 우리가 한다**. 같은 이미지를 국소
 * 임계로 직접 이진화했더니 QR이 1.9ms에 읽혔다 — 정보는 처음부터
 * 남아 있었고 막힌 것은 이진화 단계였다.
 *
 * [임계 규칙 둘 — 둘 다 필요하다] midpoint 인자로 고른다.
 *   false: 국소 **평균**(반경 block/2 박스). 
 *   true : 국소 **중간값**((min+max)/2, 구조 블록에서만 가져옴).
 * 서로 다른 심볼로지를 살린다. 실측(대비 0.05~0.40 8단, module 8):
 *
 *   심볼로지    평균만  중간값만  둘 다
 *   Code128      8/8     6/8     8/8
 *   EAN13        8/8     6/8     8/8
 *   DataMatrix   8/8     6/8     8/8
 *   UPCA         8/8     6/8     8/8
 *   QR           6/8     8/8     8/8
 *   EAN8         8/8     8/8     8/8
 *
 * 왜 갈리는가. 평균은 굵은 요소가 속이 빈다 — 창이 통째로 바 안에
 * 들어가면 평균이 바 자신의 밝기가 되어 임계가 그 위아래로 흔들린다
 * (ITF 굵은 바가 윤곽선만 남았다). 중간값은 그 자리를 확실히 검정으로
 * 만들지만, 라벨 종이 가장자리처럼 밝기가 한 번 꺾이는 자리에서
 * 코드 쪽으로 값이 새어 QR의 밝은 모듈까지 검게 만든다.
 * 여기는 다른 게 다 실패한 뒤의 구제 자리이므로 호출부가 둘 다 돌린다.
 *
 * [구조 마스크가 반드시 필요하다] 균일한 종이를 임계로 가르면 노이즈가
 * 반반 갈려서 없던 무늬가 생긴다. 실제로 그렇게 만들었더니 DataMatrix의
 * 정지대가 통째로 검게 칠해져 L-파인더 탐지가 죽었다. 마스크는
 * 닫힘(팽창 후 침식)으로 만든다 — 팽창만 하면 코드 바깥 한 블록 띠에
 * 소금후추가 생겨 QR 정지대가 깨진다.
 *
 * block: 국소 통계의 격자 크기(원본 픽셀). 평균 창은 이 값의 절반
 *   반경이다. 모듈보다는 커야 하고(그래야 임계가 밝은/어두운 요소의
 *   사이에 놓인다), 너무 크면 창 안에 종이 배경이 섞여 임계가 어긋난다.
 *   실측(대비 0.10, module 8):
 *     block   24  32  48  64
 *     Code128  O   O   O   X
 *     QR       O   O   X   X
 *     DM       O   X   X   X
 *   셋 다 통과하는 24가 기본값이다.
 * minRange: 블록 명암 폭이 이 미만이면 "구조 없음"으로 본다.
 * [[vscan-lite-roi-local-binarize]]
 */
bool localAdaptiveBinarize(const GrayView& src, GrayImage& out, bool midpoint,
                           int block = 24, int minRange = 16);

/*
 * 내용이 실제로 들어있는 범위를 찾아 그만큼만 잘라낸다(여백 marginPx).
 * 잘라낼 게 없으면(내용이 거의 전체) false.
 *
 * 용도는 **회전 구제 뒤 재크롭**이다. 기울어진 코드를 담으려면 크롭이
 * 대각선 길이만큼 커야 하는데, 되돌리고 나면 코드는 축에 정렬돼서
 * 그 상자의 20% 남짓만 차지한다. 나머지는 회전으로 생긴 흰 여백이라
 * 디코더가 훑을 이유가 없다.
 *
 * 실측(Code128 모듈 8px, 회전 25/40/70도): 회전본 1184x1088에서 실제
 * 코드는 882x320(면적의 22%)이었고, 그만큼만 잘라 풀옵션으로 디코드하면
 * 16.8 -> 9.8ms다(검출은 동일).
 *
 * 임계값은 이미지 자신의 명암 범위에서 잡는다 — 노출이 프레임마다
 * 다르므로 고정 상수를 쓸 수 없다. 회전으로 생긴 여백은 흰색(255)이라
 * 자연스럽게 배경 쪽으로 떨어진다.
 * [[vscan-lite-retight-after-rotate]]
 */
// offX/offY를 주면 잘라낸 원점을 돌려준다(원본 좌표 -> 결과 좌표 보정용).
// 여백은 상하좌우가 대칭이 아닐 수 있으므로 폭/높이 차이로 되짚으면 틀린다.
bool tightenToContent(const GrayView& src, GrayImage& out, int marginPx = 24,
                      int* offX = nullptr, int* offY = nullptr);

/*
 * [저대비 코드 경계 찾기 — tightenToContent()가 안 듣는 자리]
 *
 * `tightenToContent()`는 크롭 전체의 min/max로 임계를 잡는 전역 방식이라
 * **저대비 코드에는 원리적으로 안 듣는다** — 명암 폭이 24 미만이면 아예
 * false를 돌려주고, 크롭에 고대비 장면이 섞여 있으면 그 장면 쪽으로
 * 상자가 잡힌다.
 *
 * 여기서는 밝기가 아니라 **국소 변동**을 본다. blockPx 격자로 나눠
 * 각 칸의 (최대 - 최소)를 재고, 그 값이 minRange 이상인 칸들 중 가장 큰
 * 연결 덩어리의 상자를 돌려준다. 코드는 모듈 경계마다 변동이 있으므로
 * 대비가 낮아도 "변동이 있는 칸"으로 남는다 — 실측(DataMatrix module 6,
 * 대비 0.05): 코드 자체의 변조가 13계조뿐이지만 배경(평탄한 종이)은
 * 3계조라 갈린다.
 *
 * offX/offY에 잘라낸 위치를 돌려준다(좌표 보정용).
 * [[vscan-lite-lowcontrast-tighten]]
 */
bool tightenToLocalVariation(const GrayView& src, GrayImage& out, int blockPx = 8,
                             int minRange = 6, int marginPx = 8,
                             int* offX = nullptr, int* offY = nullptr);

/*
 * 정수배 확대(쌍선형) + 언샤프 마스킹.
 *
 * 용도는 **작은 코드 구제**다. 모듈이 2px 안팎이면 인쇄/광학 흐림이
 * 모듈 경계를 뭉개서 이진화가 어느 쪽으로도 안 떨어진다. 확대만으로는
 * 흐림도 같이 커질 뿐이라, 확대한 뒤 고주파를 되살려야 한다.
 *
 * 실측(실물 카메라 해상도 차트, 3.1MP 2000x1500, QR 21x21모듈이 약
 * 45px = 모듈 2.1px, 사본이라 흐림이 심함) — 코드 주변을 잘라놓고:
 *   원본 크기 그대로   24곳 중  0곳
 *   확대만(2/3/4/6배)  24곳 중  3곳
 *   확대 + 언샤프      24곳 중  9곳 (배율별 최선)
 *   3배 + 언샤프 고정  24곳 중  6곳
 * 성공 조합의 거의 전부에 언샤프가 들어 있었다 — 배율보다 샤프닝 유무가
 * 결정적이었다.
 *
 * amount는 고주파를 더하는 세기(100 = 원본 + (원본-블러) 1.0배).
 * [[vscan-lite-small-code-upscale]]
 */
void upscaleSharpen(const GrayView& src, int factor, GrayImage& out, int amount = 150);


} // namespace vscan

