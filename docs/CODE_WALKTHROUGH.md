# vscan-lite 코드 해설 — 첫 커밋부터 지금까지

이 문서는 두 부분이다.

- **1부**는 코드를 직접 뜯어본다. 어떤 파일이 무엇을 하고, 프레임 한 장이
  들어와서 답이 나올 때까지 어떤 단계를 어떤 순서로 지나는지, 그 단계들이
  **왜 그렇게 생겼는지**를 중간 과정까지 적는다.
- **2부**는 같은 내용을 열두 살에게 설명하듯 다시 쓴다. 용어를 하나도
  안 쓰고, 비유로만 간다.

둘 다 읽을 필요는 없다. 코드를 만질 사람은 1부, "대체 뭘 만든 거냐"가
궁금하면 2부만 봐도 된다.

기준 시점: 커밋 187개 (2026-07-30 `first commit` ~ 2026-08-16).

---

# 1부 — 코드를 직접 뜯어본다

## 1.1 이 물건이 무엇인가

**한 줄**: 카메라가 찍은 흑백 사진 한 장을 받아서, 그 안에 있는 바코드를
전부 찾아 읽고, 읽은 문자열과 위치를 돌려주는 C++ 라이브러리다.

**돌아가는 곳**: i.MX8M Plus라는 산업용 보드. ARM 코어 4개, 우리가 개발에
쓰는 x86 PC보다 **약 8배 느리다**. 이 8배는 이 저장소 전체에서 계속 나오는
숫자라 기억해 두면 좋다 — 문서의 모든 시간은 x86 실측이고, 보드 값은 ×8이다.

**바탕**: zxing-cpp라는 오픈소스 바코드 라이브러리를 감싼다. 다만 "감싼다"가
얇은 래퍼라는 뜻은 아니다. zxing-cpp에 그냥 사진을 주면 **실패한다** —
이 저장소의 코드 13,570줄 중 대부분은 "zxing이 읽을 수 있는 상태로 사진을
만들어 주는" 일과 "여러 번 시도하되 시간 안에 끝내는" 일이다.

```
사진 (2048x1536 회색조)
   │
   ├─ [우리 코드] 조건을 재고, 고치고, 자르고, 돌리고, 확대하고…
   │
   └─→ zxing-cpp ─→ "VSCAN-SWEEP-01" + 네 꼭짓점 좌표
```

## 1.2 파일 지도

```
include/
  vscan.h                    865줄  ← 공개 C API. 밖에서 보이는 전부.
  vscan_internal/
    pipeline.hpp            1038줄  ← 설정 구조체 + 파이프라인 선언
    preprocess.hpp           408줄  ← 전처리 함수 선언
    locate.hpp               142줄  ← 코드 위치 찾기 선언
    decoder.hpp                55줄  ← 디코더 인터페이스(추상)
    frame.hpp                        ← GrayView (복사 없는 이미지 뷰)

src/
  pipeline.cpp              3563줄  ← ★ 심장. 단계 순서와 구제 사다리 전부
  preprocess.cpp            1141줄  ← 이진화·디노이즈·평탄화·확대
  decoder_dotcode.cpp        926줄  ← DotCode (자체 구현, 오픈소스 없음)
  decoder_micropdf417.cpp    945줄  ← MicroPDF417 (자체 구현)
  decoder_postal.cpp         564줄  ← 일본우편 / USPS IMB (자체 구현)
  decoder_linear.cpp         464줄  ← 2of5 계열, Pharmacode (자체 구현)
  locate_qr.cpp              421줄  ← QR 파인더 패턴으로 직접 찾기
  deskew_persp.cpp           326줄  ← 원근 펴기(호모그래피)
  locate.cpp                 307줄  ← 기울기 에너지로 코드 후보 영역 찾기
  gs1_composite.cpp          276줄  ← GS1 합성 심볼 2D 성분
  pitch_equalize.cpp         256줄  ← 원통 라벨의 바 간격 균등화
  vscan_c_api.cpp            259줄  ← C++ → C 경계
  deskew1d.cpp               201줄  ← 1D 기울기 보정
  decoder_zxing.cpp          147줄  ← zxing-cpp 어댑터
  capture_v4l2.cpp           172줄  ← 카메라 캡처(리눅스 V4L2)

tools/                              ← ★ 이 저장소의 절반은 측정 도구다
  generate_corpus.py        2023줄  ← 테스트 사진 생성기
  verify_accuracy.cpp        755줄  ← 채점기
  field_diagnose.cpp         620줄  ← 현장 자가진단
  ci_gate.sh                 607줄  ← 회귀 게이트 12단계
  fulltest_100k.sh           228줄  ← 10만 장 풀테스트
  fulltest_report.py         249줄  ← 보고서 생성기
  audit_config.cpp           250줄  ← 공개 손잡이가 실제로 동작하는지 감사
```

**여기서 이미 한 가지가 보인다**: 라이브러리 본체가 13,570줄인데 측정
도구가 7,878줄이다. 거의 절반이다. 이건 사고가 아니라 이 프로젝트의
성격이다 — 아래 1.9에서 왜 그렇게 됐는지 나온다.

## 1.3 프레임 한 장이 지나가는 길

핵심 함수는 `Pipeline::processView()` (src/pipeline.cpp:558)다. 단계 이름은
`VSPROF=1` 환경변수를 주면 실제로 찍힌다:

```
[prof] 성공:denoise 합계 787.3ms | ts:coarse=1.2 ts:fast=8.2 rg:crop=15.4
       rg:persp=1.3 rg:pitch=0.1 rg:invert=14.3 rg:contrast=232.4
       ts:region=275.3 ts:harder=37.2 ts:invert=41.2 pre=0.4 core=76.3
       tilefb=71.6 dn:filter=12.5
```

순서는 이렇다. **위에서 성공하면 아래는 안 돈다** — 그게 이 구조의 전부다.

```
 0. frameHasStructure()   빈 프레임이면 즉시 끝    (컨베이어 공백: 36ms → 0.1ms)
 1. pre                   전처리 — 조건을 재고 필요할 때만 고친다
 2. core                  타일로 쪼개 병렬 디코드   ← 대부분 여기서 끝난다
 3. tilefb                타일보다 큰 코드용 전체 스캔
 4. dn:filter/decode      뭉개서 다시 (노이즈 구제)
 5. ff:filter/decode      조명 평탄화 후 다시 (그림자 구제)
 6. dpm                   도트 각인 구제
 7. rg:*                  영역 구제 — 후보 영역을 잘라서 확대/회전/원근/곡면
 8. qrfinder              QR 파인더 패턴으로 직접 찾기 (기본 꺼짐)
 9. deskew1d              1D 기울기 보정
```

### 왜 사다리인가

단순히 "다 해보면 되지 않나"가 안 되는 이유는 **시간**이다. 산업 현장은
컨베이어가 돌고 있고, 프레임 하나에 쓸 수 있는 시간이 정해져 있다.
그런데 위 단계를 전부 돌면 x86에서 800ms, 보드에서 6.4초다.

그래서 구조가 이렇다:

- **쉬운 프레임은 2단계에서 끝난다** (x86 5~20ms)
- **어려운 프레임만 사다리를 내려간다**
- **마감(`max_frame_ms`)을 걸면 중간에 끊는다**

실측으로 보면 이 분포가 극단적이다. 300장 코퍼스에서 중앙값(p50)은 5ms인데
p95는 300ms다. **60배 차이**다. 평균만 보면 이 구조를 이해할 수 없다.

### 단계 하나를 예로 — `core`

```cpp
std::vector<PipelineResult> Pipeline::processViewCore(const GrayView& image,
                                                      bool tileFallback) {
    // 프레임을 가로로 잘라 타일 N개로 나누고 스레드 N개로 병렬 디코드
    // 타일 사이에 tileOverlapPx(기본 500px) 겹침을 둔다
```

겹침이 500px인 이유가 재밌다. 겹침이 코드 세로 크기보다 작으면 코드가
타일 경계에서 **잘려서** 어느 타일에서도 안 읽힌다. 실기 차트의 QR이
세로 460~600px이라 500px + 여유로 정했다.

그런데 겹침을 크게 두면 같은 코드가 **두 타일에서 각각 읽혀** 중복이 된다.
그래서 `dedup()`이 필요해졌고, dedup이 지금 이 코드베이스에서 가장 규칙이
많은 함수 중 하나가 됐다(아래 1.6).

## 1.4 전처리 — "조건을 먼저 재고, 맞는 것만 한다"

`preprocessFrame()` (src/pipeline.cpp:666 부근)은 이 프로젝트의 설계 철학이
가장 잘 드러나는 자리다. 초기 버전은 이랬다:

```
[낡은 방식] 모든 프레임에 디노이즈 + 평탄화 + 이진화를 다 건다
```

이러면 깨끗한 프레임까지 비용을 낸다. 그래서 커밋 `338bb0d`(수렴 전략
S1~S4)에서 뒤집었다:

```cpp
const float noise = estimateNoise(image);      // 먼저 잰다
if (noise > threshold) {                        // 필요할 때만
    boxBlur3x3(image, denoiseBuf_);
    preDenoised_ = true;
}
```

결과: 최악 프레임과 최선 프레임의 시간 차이가 **69배 → 2.8배**로 줄었다.

### 이진화 — 이 저장소가 zxing을 못 믿는 첫 자리

바코드 디코딩은 결국 "이 픽셀이 검정인가 흰색인가"로 시작한다. 그런데
조명이 고르지 않으면 전역 임계값 하나로는 안 된다.

`preprocess.cpp`에는 임계 방식이 여러 개 있고, 실측으로 고른 순서가 있다:

| 방식 | 언제 이기나 |
|---|---|
| 전역 히스토그램 | 깨끗하고 균일한 조명 |
| 블록 적응 | 조명 기울기 |
| **행 단위 적응** | 대비 0.05 같은 극저대비 ← 커밋 `8354a18` |
| 세로 평균 선행 | 1D에서 세로 방향 노이즈 |

커밋 `8354a18`의 제목이 "대비 축 100%: 임계를 블록이 아니라 **행 단위**로
잡는다"다. 1D 바코드는 세로로 같은 무늬가 반복되므로, 행 하나 안에서
임계를 잡으면 조명 기울기를 자동으로 상쇄한다. 블록 단위로는 블록 경계에서
임계가 튀어서 바가 끊긴다.

## 1.5 코드를 어떻게 찾는가 — 로케이터

`findCodeRegions()` (src/locate.cpp)의 원리는 단순하다:

1. 이미지를 4배 축소한다 (빠르게 하려고)
2. 32×32 타일로 나눈다
3. 각 타일의 **기울기 에너지**(gradient energy)를 잰다
4. 프레임 최대 에너지의 20% 이상인 타일만 남긴다
5. 남은 타일을 이어 붙여 영역으로 만든다

바코드는 흑백 경계가 촘촘해서 기울기 에너지가 높다. 배경(종이, 벽)은 낮다.
그래서 잘 동작한다 — **노이즈가 없으면**.

### 여기서 이번 세션에 찾은 결함 (§3.108)

4번의 "프레임 최대 에너지의 20%"가 **상대 기준**인 것이 문제다. 센서
노이즈가 얹히면 배경 타일의 에너지가 통째로 올라가서 거의 모든 타일이
임계를 넘는다. 그러면 영역이 하나로 뭉쳐 **프레임 전체 상자**가 나온다.

실측 — 같은 코드, 크기도 같고, 노이즈만 다르다:

| | 영역 크기 |
|---|---|
| DataMatrix 60×60, 노이즈 0 | **256×256** (정확) |
| DataMatrix 60×60, 노이즈 25 | **2048×1536** (프레임 전체) |

프레임 전체 상자를 크롭해 봐야 원본과 같으니 아무것도 못 번다. 그래서
"코드를 못 찾는다"가 아니라 "찾았다고 하는데 쓸모가 없다"는 형태로 실패한다.

**고친 방식**: 결과가 이미 쓸모없을 때만(영역이 프레임의 55% 초과) 임계를
0.65로 올려 다시 찾는다. 처음부터 올리면 코드가 여럿인 프레임에서 약한
코드가 강한 코드에 밀려 후보에서 빠진다.

되살리는 로케이트는 **타일 16 / 축소 2**로 돈다. 임계만 올리면 상자가 타일
격자만큼 헐렁해서(60×60 코드가 256×256 상자) 그 크롭이 다시 실패하기
때문이다:

| 크롭 크기 | 결과 |
|---|---|
| 256×256 | 미검출 |
| 192×192 | 읽음 |
| 96×96 | 읽음 |

노이즈 프레임에서는 **코드 밖 면적이 곧 오검출 기회**다.

## 1.6 중복 제거 — 규칙이 여섯 개인 이유

같은 코드가 여러 번 나오는 경로가 많다:

- 타일 겹침에서 두 번
- 원본과 구제본에서 각각
- zxing과 ZBar 둘 다 붙였을 때

그래서 `dedup()`이 필요한데, "같은 텍스트면 하나로" 만으로는 안 된다.
**박스에 같은 라벨이 두 장 붙어 있는 배치**가 실제로 있기 때문이다.
그건 진짜 코드 두 개다.

지금 있는 규칙들:

| 규칙 | 무엇을 잡나 |
|---|---|
| `dedup-degenerate-quad` | 꼭짓점이 겹쳐 위치를 못 믿는 결과 |
| `dedup-partial-scan` | 한쪽 텍스트가 다른 쪽의 부분 문자열 (ITF 20/70도) |
| `dedup-thin-slice` | 두께가 상대의 35% 미만인 스캔 조각 |
| `dedup-stacked-band` | 세로로 붙은 밴드 |
| `dedup-twin-band` | 같은 코드를 위아래로 스친 두 밴드 |
| `dedup-single-band` | 자기 길이의 15% 이하 두께 = 스캔 밴드 |

각 규칙에는 **양성 대조**가 붙어 있다. `tools/generate_stacked_labels.py`가
같은 라벨 2장을 간격을 바꿔가며 만들고, 게이트가 "간격 240px면 둘 다
나와야 한다"를 지킨다. 규칙을 세게 만들면 이 대조가 먼저 깨진다.

## 1.7 이번 세션에 추가된 것 — 저해상도 1D 강등

풀테스트에서 오디코딩(없는 코드를 만들어내는 것) 88건을 한 건씩 재생성해
확인했더니 이런 게 나왔다:

```
ITF '681731'   ← 정답 '867935681731' 의 뒤 6자리
ITF '31803327' ← 정답 '7631803327'   의 뒤 8자리
```

**처음 진단은 틀렸다.** "바 열을 끝까지 못 훑은 부분 스캔"이라고 보고
정지대(quiet zone) 검사를 넣었는데 87건 중 3건만 걸렸다.

한 장을 계측해서 전제를 다시 쟀다:

```
[qz] quad (500,159) (406,159) (405,161) (499,161)   L=94
[qz] 관측 요소 36개                    ← 6자리 ITF의 기대값이 37개
[qz] side-1 전이=1   side+1 전이=0     ← 양쪽 다 정지대다
```

세 가지가 한꺼번에 전제를 무너뜨렸다. 상자가 코드 **전체**를 덮고 있고,
양 끝이 정지대이고, 요소 개수가 6자리와 **일관**된다. 즉 디코더는 자기가
본 것과 맞는 답을 냈다. 틀린 것은 **본 것 자체**다.

이 결과는 3배 축소된 뷰에서 나왔고 거기서 좁은 요소가 **1.0px**이었다.
12자리(요소 67개)가 94px에 들어가면 요소당 1.4px이라 이웃한 좁은 요소들이
뭉개진다. 뭉개진 무늬를 ITF 문법으로 읽으면 원본의 **부분 수열**이 나온다 —
그래서 결과가 정답의 꼬리처럼 보였던 것이다.

요소 하나를 가르려면 표본이 최소 둘은 있어야 한다(나이퀴스트). 이 저장소가
§3.61에서 **따로** 찾은 "모듈 2px 벽"과 같은 수다. 두 경로로 같은 수에
도달한 것이 이 진단의 근거다.

**그런데 버리면 손해였다:**

| 모드 | 텍스트 일치 | 오디코딩 |
|---|---|---|
| 끔 | 42.23% | 25 |
| 버림 | 41.86% | 26 |
| 프레임 단위 강등 | 41.89% | 22 |
| **코드 단위 강등** | **42.71%** | 23 |

세 번째와 네 번째의 차이가 내가 짠 규칙의 결함이었다. 강등이란 "이 코드에
더 나은 답이 있으면 물러난다"인데 "**프레임 어딘가에** 더 나은 답이 있으면
물러난다"로 짰다. 난수 코퍼스는 한 장에 코드가 최대 12개라, 다른 코드를
맞게 읽은 약한 결과까지 같이 버리고 있었다. 245코드를 그렇게 잃고 있었다.

**정직하게 적어둘 것**: 이 변경은 오디코딩을 겨냥했고 그 축에서는 아무것도
못 했다(25 → 23은 n=25의 푸아송 잡음 안이다). 값을 낸 곳은 검출이고 경로도
의도와 달랐다 — 해상도 미달 1D 값 하나로 프레임이 끝나지 않게 되니
파이프라인이 계속 찾는 것이다.

## 1.8 설정 — 손잡이 38개와 "죽은 손잡이" 문제

`include/vscan.h`에 공개 설정이 38개 있다. 전부 0으로 초기화하면 기본값이다.

주요한 것들:

| 손잡이 | 무엇 |
|---|---|
| `min_expected_codes` | 이 프레임에 코드가 몇 개인지 안다면 알려준다 |
| `auto_expected_codes` | 모르면 프레임에서 추정하게 한다 (39.2% → 70.3%) |
| `max_frame_ms` | 벽시계 마감 |
| `two_stage_self_budget` | 상대 마감 (하드웨어가 바뀌어도 뜻이 안 바뀜) |
| `fast_no_read` | "읽지 말고 있는지만 판정" 모드 |
| `symbology_mask` | 안 쓰는 심볼로지는 아예 찾지 않기 |
| `enable_locate_escalation` | 이번에 추가 (기본 꺼짐) |

### 죽은 손잡이

커밋 `0c25d0a`, `c6fa51f`, `3222367`의 제목이 이렇다:

```
공개 플래그 둘이 2단계 경로에서 조용히 무시되고 있었다
공개 설정 전수 감사 도구 — 죽은 손잡이를 두 개 더 찾았다
손잡이 생존 확인을 게이트에 걸고, 저조도 기준을 올렸다
```

문서에 있고 헤더에 있는데 **실제로는 아무 일도 안 하는** 설정이 넷 있었다.
그래서 `tools/audit_config.cpp`를 만들었다 — 손잡이를 켜고 끄면서 결과가
실제로 달라지는지 확인하고, 안 달라지면 실패한다. 지금 게이트 [3.8]이
6종을 지킨다.

## 1.9 측정 도구가 절반인 이유 — 하네스 결함 여덟 번

이 프로젝트에서 **가장 자주 틀린 것은 라이브러리가 아니라 테스트였다.**
지금까지 여덟 번이다:

| # | 무엇 | 증상 |
|---|---|---|
| 1 | 원근 (§3.29) | 생성기가 코드를 캔버스 밖으로 잘라내고 있었다 |
| 2 | 반전 (§3.41) | 정지대가 규격 미달이라 "반전 실패"가 아니라 "정지대 파괴"를 재고 있었다 |
| 3 | DPM (§3.83) | 도트 각인 격자 간격이 틀렸다 |
| 4 | DPM (§3.97) | **같은 축에서 또** — 격자 위상과 누적 드리프트 |
| 5 | 정지대 (§3.98) | 픽셀 단위 패딩이 규격 정지대를 덮어썼다 |
| 6 | 노이즈 바닥 (§3.102) | 주 코퍼스에 센서 노이즈가 아예 없었다 (합성의 티) |
| 7 | 재개 로직 (§3.104) | 쓰다 만 CSV를 완성으로 읽었다 |
| 8 | 모듈 축 (§3.105) | 넓은 1D에서 모듈 축이 통째로 죽어 있었다 |

**8번을 자세히 보자** — 이번 세션에 찾았고, 하마터면 틀린 결론을 낼 뻔했다.

풀테스트에서 CODE39와 CODABAR의 기준 모듈 4와 8 결과가 **소수점까지 같았다**:

| 격자 | CODABAR m4 / m8 | CODE39 m4 / m8 |
|---|---|---|
| 대비×밝기 | 97.8 / 97.8 | 95.0 / 95.0 |
| 노이즈×블러 | 100.0 / 100.0 | 56.1 / 56.1 |
| 오염×손상 | 100.0 / 100.0 | 90.5 / 90.5 |

CSV를 통째로 비교하니 **바이트 단위로 같았다.** 원인은 생성기의 이 세 줄이었다:

```python
span = max((gw + 2 * quiet), (gh + 2 * quiet) if is2d else 0)
if span * mod > box:
    mod = box / span          # 셀에 안 들어가면 말없이 줄인다
```

폭이 넓은 1D는 이 클램프에 **항상** 걸려서 요청 4든 8이든 같은 값(3.66px)
으로 포화했다. 진짜 문제는 중복 계산이 아니라 **비교가 깨진 것**이다 —
같은 "모듈 8" 칸에서 EAN13은 8.00px, CODE39는 3.66px을 받는다. 그 상태로
검출률을 나란히 놓은 표는 심볼로지가 아니라 **크기**를 비교한다.

그리고 나는 실제로 그 표를 읽고 "CODE128은 블러에 약하다"라고 결론 낼
뻔했다. 라이브러리 문서에 심볼로지 권고로 남을 문장이었다.

**고친 방식**: 모듈을 깎지 말고 **캔버스를 넓힌다**. 코드와 프레임의 비율이
그대로라 "모듈당 표본 수"라는 축만 혼자 움직인다. 상한(12MP)에 걸리면
예전처럼 깎이지만, 그때는 태그에 실제 모듈이 찍힌다:

```
angle=0,module=8,sym-CODE_93,modpx-6.27,img-ok
                             ^^^^^^^^^^ 요청 8인데 6.27을 받았다
```

> **규칙: 요청값과 실제값이 다를 수 있으면, 실제값을 산출물에 적을 것.**
> 생성기가 물리적 제약 때문에 요청을 못 지키는 것 자체는 정상이다. 문제는
> 그 사실이 결과물 어디에도 안 남는 것이다.

## 1.10 회귀 게이트 — 12단계

`tools/ci_gate.sh`가 지키는 것들:

| 단계 | 무엇을 지키나 |
|---|---|
| [2] 고정 40종 | 세 경로 전부 40/40 |
| [3] 심볼로지 각도 스윕 | 14종 × 19각도 100%, 오디코딩 0 |
| [3.5] 세밀 각도 (1도) | 오디코딩 상한 고정 (ITF 13, CODE128 2) |
| [3.7] 저조도 SNR | 코드 하한 28, 평균 상한 130ms |
| [3.73] 권장 조합 회전 | QR/CODE128/PDF417 100% |
| [3.74] **실물 FOV 차트** | 이 저장소의 유일한 실사 프레임, 하한 5개 |
| [3.75] 빈 프레임 유령 | 19장에서 코드 0개 |
| [3.78] 같은 라벨 2장 | dedup 양성 대조 |
| [3.781] 로케이터 되살리기 | 켜면 벌고 끄면 예전 그대로 (양방향) |
| [3.77] DPM 축 | 실측 기준선 + 오디코딩 상한 1 |
| [3.771] 손상/오염/인쇄불량/정지대 | 6종 최저 100% |
| [3.76] 컨베이어 판정 최대 | 잡동사니 빈 프레임에서 |
| [3.79/3.795] 자동 기대 개수 | 검출·오디코딩·p95 셋 다 |
| [3.8] 손잡이 생존 | 6종이 실제로 동작하는가 |
| [4/5] 고정 시드 코퍼스 | 기준선 대비 회귀 |

### [3.5]가 왜 따로 있나

[3] 각도 스윕이 "오디코딩 0"이라고 말하지만 그건 **5도 배수 각도에서만**이다.
1도 간격으로 다시 재면 다르다:

```
ITF      5도 간격 19장 → 오디코딩 1 / 1도 간격 91장 → 오디코딩 13
CODE128  5도 간격      → 0          / 1도 간격      → 2 (44도와 46도)
```

45도 정각에서는 정상이라 5도 격자가 **원리적으로 못 본다**. 이걸 모른 채
"오디코딩 0"이라고 적어둘 수는 없었다.

### 기준 작업량 정규화

게이트가 시간을 절대 ms로 비교하면 안 된다. 같은 코드인데 샌드박스가
느려져서 게이트가 떨어진 적이 실제로 있다. 그래서 40종 세 경로 합계를
"이 머신이 지금 얼마나 빠른가"의 척도로 쓰고, 코퍼스 시간을 그 값으로
나눈다.

이번 세션에 이게 값을 했다. 로케이터 되살리기를 기본으로 켜려 했는데:

| | 300장 원시 p95 | 기준 정규화 p95 |
|---|---|---|
| 끔 | 264ms | 139.3 |
| 켬 | 306ms (+16%) | **174.0 (+25%)** |

원시 숫자만 보면 "+16%, 그럴 만하다"인데 정규화하면 25% 회귀다. 게이트가
두 번 떨어뜨렸고, 결국 **기본 꺼짐 옵션**으로 냈다.

## 1.11 테스트 사진 생성기 — 2,023줄짜리 물건

`tools/generate_corpus.py`가 하는 일:

1. **심볼로지 14종**을 인코딩한다 (라이브러리 넷 + 자체)
2. 모듈 격자(불리언 행렬)로 정규화한다
3. 원하는 모듈 픽셀 크기로 **면적 샘플링**해서 그린다
4. 열화를 얹는다 — 축이 21개
5. 스트리밍으로 stdout에 흘린다 (디스크 0)

### 축 21개

```
sym  ec  count  module  angle  contrast  bright  blur  motion  noise
persp  curve  glare  shadow  invert  dpm  printdefect  damaged
quietzone  dirty  clutter
```

### 왜 모듈 격자로 정규화하나

인코더마다 출력이 제각각이다(비트열/PIL 이미지/행렬). 이미지를 그냥
리사이즈하면 모듈 폭 비율이 깨진다. 그래서 전부 모듈 격자로 되돌린 다음
우리가 원하는 크기로 다시 그린다.

되돌리는 방법이 재밌다 — **최소 런 길이 = 모듈 하나의 픽셀 크기**라는
성질을 쓴다:

```python
u = max(1, min(min_run(a), min_run(a.T)))     # 모듈은 정사각이라고 본다
h = max(1, int(round(a.shape[0] / u)))
w = max(1, int(round(a.shape[1] / u)))
```

### 센서 노이즈 바닥 (§3.102)

이 코퍼스에는 오래 **노이즈가 아예 없었다**. `noise`가 축으로만 있고
기본값이 0이었다. 그런데 실제 카메라는 노이즈가 0인 적이 없다.

```python
def sensor_noise_floor(arr, rng, extra_sigma=0.0):
    read = 2.2                                    # 읽기 노이즈
    shot = np.sqrt(np.maximum(arr, 0.0)) * 0.16   # 광자 산탄 노이즈
    sigma = np.sqrt(read ** 2 + shot ** 2 + float(extra_sigma) ** 2)
    return arr + rng.normal(0, 1, size=arr.shape) * sigma
```

이걸 넣으면서 검출률이 0.7%p 떨어졌다. 그 0.7%p는 **잃은 성능이 아니라
과대평가였던 부분**이다.

> 규칙: **"항상 있는 것"을 옵션으로 두지 말 것.** 렌즈 MTF 블러는 이미
> 항상 걸고 있었는데 센서 노이즈만 축이었다. 둘 다 물리적으로 항상 있는
> 것이라 같은 대우를 받아야 한다.

### 판독 가능성 버킷 — 그리고 이번에 고친 것

`classify_code()`가 코드 하나를 `ok` / `borderline` / `impossible`로 나눈다.
"물리적으로 읽을 수 있는가"를 판정하는 것이라, 성능 주장은 **`ok` 행에서만**
해야 한다.

그런데 이 분류기가 **손상과 오염을 아예 안 보고 있었다**. 90%가 지워진
QR도 `img-ok`였다. 생성기 주석에 그렇게 적혀 있었는데
(`[[vscan-lite-sweep-damage-uncalibrated]]`), 내가 풀테스트 보고서를 쓰면서
그 경고를 어기고 F격자 절대값을 성능처럼 실었다 — **QR 14.7%**가 그것이다.

**고친 방식**: 파괴량을 모델링하지 않고 **잰다**. 손상을 그리기 전후를 빼면
정확한 값이 나온다:

```python
before = np.array(sym, dtype=np.int16)
after  = np.array(im2, dtype=np.int16)
code_px = before < 128                          # 원래 검정이던 모듈
killed  = code_px & (np.abs(after - before) > 64)
dmg["frac"] = float(killed.sum()) / max(1, int(code_px.sum()))
```

```
damaged 0.25 → 파괴 3.4%    0.50 → 11.7%
damaged 0.75 → 24.4%        1.00 → 33.6%
```

그 값을 **오류정정 용량**과 견준다 — QR은 L 7% / M 15% / Q 25% / H 30%,
DataMatrix ECC200 28%, PDF417 18%.

1D는 오류정정이 없는 대신 **세로 여유**가 있다. 스캔 라인 한 줄만 성하면
읽히므로, 면적이 아니라 "깨끗한 행이 남아있는가"로 판정한다. 물리가 다르니
판정도 달라야 한다.

실측이 이 설계를 뒷받침한다. 같은 손상 프레임에서 오류정정만 바꾸면:

| | QR 손상 축 | QR 오염 축 |
|---|---|---|
| EC L | 4/11 | 4/11 |
| EC H | 6/11 | **10/11** |

라벨이 EC를 모르면 이 차이가 통째로 "검출 실패"로 잡힌다.

**보정 후** (읽혀야 하는 프레임만):

| 심볼로지 | 보정 전(보고서) | 보정 후 |
|---|---|---|
| QR | 14.7% | **80.0%** |
| PDF417 | 66.4% | 85.7% |
| CODE128 | 74.1% | **100%** |
| CODABAR | 100% | **100%** |
| DataMatrix | 16.1% | 50.0% |

라이브러리가 좋아진 것이 아니라 **라벨이 정직해진 것**이다.

## 1.12 자체 구현한 디코더들

zxing-cpp가 안 하는 것을 직접 만들었다.

| 심볼로지 | 줄 수 | 왜 직접 |
|---|---|---|
| DotCode | 926 | **오픈소스 구현이 없다.** 인코더에서 뒤집어 만들었다 |
| MicroPDF417 | 945 | zxing v2.2.1에 별도 포맷이 없다. 표를 실측으로 뽑았다 |
| 일본우편 / IMB | 564 | 4-state 우편 바코드 |
| 2of5 계열 / Pharmacode | 464 | 표가 ITF와 전혀 다르다 |
| GS1 Composite | 276 | ISO 24723 CC-A/CC-B/CC-C |

DotCode 커밋 메시지가 이 작업의 성격을 잘 보여준다:

```
feat(dotcode): DotCode 디코더 — 오픈소스 구현이 없어 인코더에서 뒤집어 만들었다
```

## 1.13 성능 — 지금 어디쯤인가

10만 장 풀테스트(프레임 67,680장 / 코드 122,092개) 기준.

**중요**: 56.9%라는 전체 숫자는 성격이 다른 두 코퍼스를 프레임 수로
가중평균한 것이라 그 자체로는 뜻이 없다. 갈라야 보인다.

**격자 (코드 1개, 축 2개만 곱함)**

| 버킷 | 일치율 |
|---|---|
| ok (읽혀야 정상) | **86.4%** |
| borderline | 62.4% |
| impossible | 37.5% |

열화가 없으면 96.6%다.

**난수 혼합 (코드 여럿 + 열화 겹침 + 개수 안 알려줌)**

| 겹수 | 1 | 2 | 3 | 5 | 7 | 9 |
|---|---|---|---|---|---|---|
| 일치율 | 88.9% | 80.3% | 71.0% | 47.7% | 32.6% | 25.2% |

**축별 절단점** — 훑은 17개 중 11개가 끝까지 안 무너진다:

| 축 | 50%를 지나는 값 |
|---|---|
| `module` | 1.125px (물리 한계) |
| `blur` | 시그마 2.75 |
| `persp` | 0.6 |
| `damaged` | 0.75 |
| `contrast` | 0.05 |
| 각도/노이즈/반사/그림자/오염/정지대/인쇄불량/반전/DPM/곡면/개수 | **없음** |

**시간** (x86 실측 → 보드 ×8):

| | x86 | 보드 |
|---|---|---|
| 평균 | 63.1ms | 505ms |
| p95 | 318.4ms | 2,547ms |
| 컨베이어 판정 최대 | 11.5ms | 92ms |

## 1.14 이 저장소의 작업 규칙

`PROJECT_NOTES.md` §7에 쌓인 규칙들. 전부 데어서 생겼다.

1. **미검출 < 오디코딩.** 못 읽는 것보다 잘못 읽는 것이 나쁘다.
2. **측정하고 나서 말할 것.** 가설은 절반이 틀린다.
3. **안 되는 것 목록은 빨리 썩는다.** 전제가 바뀌면 다시 재라.
4. **규칙에는 양성 대조를 붙일 것.**
5. **숫자를 찍는 자리마다 단위를 같이 찍을 것.** (게이트가 x86 값에
   "보드 환산" 라벨을 붙이고 있었다)
6. **요청값과 실제값이 다를 수 있으면 실제값을 남길 것.**
7. **규칙이 거의 안 걸리면 문턱이 아니라 전제를 의심하라.**
8. **부분적으로 쓰인 파일의 존재를 완료 신호로 쓰지 말 것.**

7번은 이번 세션에 세 번 값을 했다:

| 의심한 문턱 | 만지기 전에 잰 결과 |
|---|---|
| 정지대 규칙 | 87건 중 3건만 걸림 → 전제가 틀렸다 |
| 확대 구제 (모듈 기반) | 0건 개선, QR 25ms → 282ms → 되돌림 |
| 곡면 보정 문턱 | 1.25/1.10/1.00 전부 26/42 → 지렛대 아님 |


---
---

# 2부 — 열두 살에게 설명한다면

여기서부터는 어려운 말을 하나도 안 쓴다. 1부를 안 읽어도 된다.

## 2.1 바코드가 뭐냐면

과자 봉지 뒤에 있는 검은 줄무늬. 계산대에서 "삑" 하는 그거다.

그 줄무늬는 사실 **숫자를 그림으로 바꿔 놓은 것**이다. 굵은 줄, 얇은 줄,
굵은 흰칸, 얇은 흰칸… 이걸 순서대로 읽으면 숫자가 나온다.

모스 부호랑 똑같다. 점과 선으로 글자를 적는 것처럼, 굵기로 숫자를 적는다.

**정사각형 모양(QR코드)도 있다.** 이건 줄이 아니라 바둑판처럼 생겼고,
훨씬 많은 정보를 담을 수 있다.

## 2.2 우리가 만든 건 뭐냐면

**사진을 보고 그 줄무늬를 읽어주는 프로그램**이다.

공장에서 쓴다. 컨베이어 벨트 위로 상자가 지나가고, 카메라가 사진을 찍고,
우리 프로그램이 "이 상자는 A-12345번이야"라고 알려준다.

## 2.3 근데 이게 왜 어렵냐면

"사진 보고 줄무늬 읽는 게 뭐가 어려워?"

여기서부터가 진짜 얘기다. **공장 사진은 깨끗하지 않다.**

### 어려움 1 — 너무 작다

멀리서 찍으면 줄무늬가 아주 얇아진다. 얼마나 얇냐면, **줄 하나가 사진에서
점 2개 크기**밖에 안 될 때가 있다.

점 2개로 굵은 줄과 얇은 줄을 구분해야 한다. 점 1개가 되면? 그땐 **아무도
못 읽는다.** 정보가 사라져 버린 거다. 이건 우리 프로그램이 못나서가 아니라
물리적으로 불가능한 거다.

> 비유: 모래알에 글씨를 쓰고 그걸 사진으로 찍었다고 생각해 보자.
> 사진이 아무리 좋아도 모래알보다 작은 글씨는 안 보인다.

### 어려움 2 — 어둡거나 너무 밝다

창고는 어둡다. 어두우면 카메라가 밝기를 억지로 올리는데, 그러면 사진에
**모래알 같은 점들(노이즈)**이 잔뜩 낀다. TV 채널 안 나올 때 지직거리는
그거다.

반대로 형광등이 반사되면 하얗게 타버려서 줄무늬가 안 보인다.

### 어려움 3 — 비뚤어져 있다

상자가 반듯하게 놓여 있을 리가 없다. 돌아가 있고, 기울어져 있고,
캔이나 병이면 **둥글게 휘어져** 있다.

둥근 캔에 붙은 라벨은 가운데는 넓고 양쪽 끝은 좁아 보인다. 그러면 줄
굵기가 위치마다 달라져서, 그냥 읽으면 틀린 숫자가 나온다.

### 어려움 4 — 더럽고 찢어져 있다

공장이니까 기름때가 묻고, 긁히고, 찢어진다.

### 어려움 5 — 시간이 없다

컨베이어는 계속 돈다. **한 장에 1초씩 쓰면 안 된다.**

### 어려움 6 — 컴퓨터가 느리다

우리가 쓰는 공장 컴퓨터는 개발용 컴퓨터보다 **8배 느리다.**
개발 컴퓨터에서 1초 걸리면 공장에선 8초다.

## 2.4 어떻게 푸느냐 — 계단 내려가기

우리 프로그램은 이렇게 생각한다:

> **"쉬운 건 빨리, 어려운 건 천천히."**

계단이 있다고 생각해 보자. 맨 위 칸이 제일 쉽고 빠르다.

```
1층: 그냥 읽어본다                      ← 대부분 여기서 끝 (아주 빠름)
2층: 사진을 조각내서 각각 읽어본다
3층: 흐릿하게 만들어서 다시 (모래알 지우기)
4층: 밝기를 고르게 펴서 다시 (그림자 지우기)
5층: 코드가 있을 만한 곳만 오려내서 크게 확대해서 다시
6층: 삐뚤어진 걸 반듯하게 펴서 다시
7층: 휘어진 걸 펴서 다시
```

**한 층에서 읽으면 아래층은 안 간다.** 그래서 깨끗한 사진은 눈 깜짝할
사이에 끝나고, 지저분한 사진만 오래 걸린다.

실제로 재보면:
- 절반의 사진: **5밀리초** (1초의 200분의 1)
- 제일 어려운 사진: **300밀리초**

**60배 차이**다.

## 2.5 제일 조심하는 것 — 거짓말

여기가 이 프로젝트에서 제일 중요한 규칙이다.

프로그램이 실수하는 방법은 두 가지다:

**(A) "못 읽겠어요"** ← 괜찮다. 다시 찍으면 된다.

**(B) "A-12345번이에요"라고 했는데 사실은 A-12399번** ← **큰일난다.**

왜 큰일이냐면, 공장에서 이러면 **엉뚱한 물건이 엉뚱한 곳으로 간다.**
그리고 아무도 모른다. 못 읽으면 사람이 알아채지만, 잘못 읽으면 모른다.

그래서 우리 규칙 1번은:

> **못 읽는 것보다 잘못 읽는 것이 나쁘다.**

프로그램이 자신 없을 때는 **차라리 입을 다물게** 만든다.

### 실제로 있었던 일

프로그램이 없는 바코드를 만들어낸 적이 있다. `676767`이라는 숫자를
읽었는데, 그 자리엔 그런 바코드가 없었다.

왜 그랬냐면 — 사진이 너무 작아서, 얇은 줄 여러 개가 **뭉쳐서 한 줄처럼**
보였다. 그 뭉쳐진 무늬가 우연히 바코드 문법에 맞았던 거다.

> 비유: 글씨가 번져서 "매"가 "떼"처럼 보이는 것. 둘 다 진짜 글자라서
> 읽는 사람은 틀린 줄 모른다.

**고친 방법**: 줄 하나가 점 2개보다 얇으면 "이건 못 믿겠다"고 표시해 두고,
더 나은 답이 나오면 그걸 버린다. 끝까지 이것밖에 없으면 그냥 준다
(없는 것보단 나으니까).

## 2.6 우리가 제일 자주 틀린 것 — 놀랍게도 시험지

여기가 이 프로젝트에서 제일 재밌는 부분이다.

시험을 보려면 문제지가 필요하다. 우리는 바코드 사진을 **컴퓨터로 만들어서**
시험 문제로 쓴다. 진짜 사진을 몇십만 장 찍을 순 없으니까.

그런데 **문제지가 틀린 적이 여덟 번**이다.

### 예시 1 — 문제지가 바코드를 잘라먹고 있었다

"비스듬히 기울인 바코드"를 만들려고 했는데, 기울이면서 **사진 밖으로
삐져나간 부분을 잘라내고** 있었다. 바코드 끝이 없어진 거다.

그래놓고 "우리 프로그램은 기울어진 바코드를 못 읽네"라고 결론 냈다.
당연하지, 바코드가 반쪽이었으니까.

### 예시 2 — 크기를 바꿔도 안 바뀌고 있었다 (이번에 찾음)

"줄 굵기 4점짜리"와 "8점짜리"를 만들어서 비교하려고 했다. 그런데 어떤
바코드는 너무 길어서 사진에 안 들어가니까, 문제지 만드는 프로그램이
**말없이 줄여서** 둘 다 3.66점으로 만들고 있었다.

즉 **똑같은 사진 두 장**을 만들어 놓고 "크기를 바꿔서 비교했다"고 하고
있었던 거다.

이게 왜 위험하냐면 — 어떤 바코드는 8점을 제대로 받고, 어떤 건 3.66점밖에
못 받는데, 그 둘의 점수를 나란히 놓고 "이 바코드가 더 약하네"라고 읽게
된다. **실제로는 크기가 다른 걸 비교한 거다.**

나는 하마터면 "CODE128이라는 바코드는 흐림에 약하다"고 결론 낼 뻔했다.

**고친 방법**: 줄여서 맞추지 말고, **사진을 크게 만든다.** 그리고 진짜로
몇 점짜리가 나왔는지 사진에 적어 둔다.

### 예시 3 — 채점 기준이 틀렸다 (이번에 찾음)

바코드를 긁어서 망가뜨린 다음 "읽어봐"라고 하는 시험이 있다.

그런데 채점표가 **얼마나 망가졌는지 아예 안 보고** 있었다. 90% 지워진
QR코드도 "이건 읽혀야 정상"으로 분류하고 있었다.

그래놓고 나는 보고서에 **"QR코드 성적 14.7%"**라고 적었다. 마치 프로그램이
못난 것처럼.

실제로는 **읽을 수 없는 걸 읽으라고 한 다음 못 읽었다고 감점**하고 있었다.

**고친 방법**:
1. 망가뜨리기 전후 사진을 빼서 **몇 %가 지워졌는지 정확히 잰다**
2. 그 바코드가 **얼마나 견딜 수 있는지**와 비교한다

QR코드는 종류에 따라 7%~30%까지 지워져도 스스로 복구할 수 있다. 찢어진
부분을 메꾸는 여분 정보를 미리 넣어두기 때문이다.

고치고 나니 QR 성적이 **14.7% → 80%**가 됐다. 프로그램이 좋아진 게 아니라
**채점이 정직해진 거다.**

> 이게 이번 점검에서 제일 중요한 발견이다. 내가 쓴 보고서의 숫자가
> 틀렸었다.

## 2.7 실수를 줄이려고 만든 습관

### 습관 1 — 문턱을 만지기 전에 "켜면 되는지"부터 본다

프로그램에는 "이 정도면 흐린 걸로 친다" 같은 기준선이 여기저기 있다.
뭐가 안 되면 그 기준선을 만지고 싶어진다.

그런데 **기준선이 문제가 아닌 경우가 훨씬 많다.**

이번에 세 번 그랬다:

| 만지고 싶었던 것 | 만지기 전에 재본 결과 |
|---|---|
| 정지대 검사 기준 | 87건 중 3건만 걸림 → 원인 자체가 딴 데 있었다 |
| 확대 기준 | 하나도 안 좋아지고 **11배 느려짐** → 되돌림 |
| 휘어짐 판정 기준 | 세 값 다 결과가 똑같음 → 여기가 아니었다 |

세 번 다 만졌으면 **느려지기만 하고 아무것도 안 좋아졌을 것**이다.

### 습관 2 — "잘 되는 경우"도 같이 확인한다

"휘어진 바코드를 잘 읽게 고쳤다!" — 좋다. 근데 **안 휘어진 바코드는
여전히 잘 읽나?**

규칙을 세게 만들면 원래 잘 되던 게 망가지는 경우가 많다. 그래서 규칙마다
"이건 반드시 되어야 한다"는 짝을 만들어 둔다.

예: 상자에 **똑같은 라벨이 두 장** 붙어 있으면 두 개 다 나와야 한다.
"같은 내용이니까 하나로 합치자"는 규칙이 이걸 망가뜨린다.

### 습관 3 — 숫자에는 항상 단위를 적는다

우리 개발 컴퓨터는 공장 컴퓨터보다 8배 빠르다. 그래서 "50밀리초"라고만
적으면 **읽는 사람이 400밀리초를 50으로 오해한다.**

실제로 그런 일이 있었다. 검사 프로그램이 개발 컴퓨터 숫자에다 "공장 기준"
이라는 딱지를 붙여 놓고 있었다. 8배 틀린 숫자였다.

## 2.8 시험은 어떻게 보나

컴퓨터로 바코드 사진을 **10만 장** 만든다. 종류 14가지 × 상황 21가지를
전부 조합한다.

만들 때 정답을 알고 있으니까, 프로그램이 읽은 걸 정답과 맞춰보면 점수가
나온다.

**사진은 저장하지 않는다.** 10만 장이면 300기가바이트라서, 만들자마자
바로 채점하고 버린다.

### 시험 결과 읽는 법

전체 점수는 **56.9%**다. 근데 이 숫자는 그냥 보면 안 된다.

왜냐면 시험 문제에 **일부러 불가능한 문제를 섞어 놨기** 때문이다.
"어디서부터 안 되는지" 경계를 찾는 게 목적이라서.

나눠서 보면 이렇다:

| 상황 | 점수 |
|---|---|
| 바코드 1개, 깨끗함 | **96.6%** |
| 바코드 1개, 읽을 수 있는 조건 | **86.4%** |
| 바코드 여러 개 + 문제 5가지 겹침 | 47.7% |
| 바코드 여러 개 + 문제 9가지 겹침 | 25.2% |

**문제가 겹칠수록 무너진다.** 흐림 하나, 어두움 하나는 각각 잘 견디는데
동시에 오면 무너진다.

그리고 재밌는 게 — 시험 본 21가지 상황 중 **11가지는 끝까지 안 무너진다.**
아무리 세게 해도 읽는다. 무너지는 것들도 대부분 **물리적으로 불가능한
지점**에서 무너진다.

## 2.9 지금까지 뭘 했나 — 187번의 저장

이 프로젝트는 187번 저장(커밋)됐다. 몇 개만 골라 보면:

```
7월 30일  first commit                    ← 시작
7월 30일  컨베이어 공백 프레임 36ms → 0.1ms
7월 31일  실물 차트 자동 검출 0곳 → 3곳
7월 31일  실물 차트 3곳 → 7곳
8월  1일  최악 프레임과 최선 프레임 차이 69배 → 2.8배
8월  1일  뒤집힌 바코드 14종 중 2종 → 13종
8월  1일  잘못 읽기 9건 → 1건
8월  2일  DotCode — 오픈소스가 없어서 직접 만들었다
8월 14일  공장 컴퓨터가 8배 느리다는 걸 알고 전부 다시 계산
8월 15일  개수를 몰라도 남은 코드를 찾게 → 40% → 73.6%
8월 16일  채점표가 망가진 정도를 안 보고 있었다  ← 이번
```

패턴이 보인다:
- **처음 열흘**: 기능 만들기 (읽는 종류 늘리기, 구제 방법 추가)
- **중간**: 속도 줄이기
- **최근**: **시험지와 채점표 고치기**

마지막이 제일 많아진 게 이상해 보이지만, 사실 당연하다. 프로그램이 어느
정도 좋아지고 나면, **틀린 건 대부분 측정 쪽**이다.

## 2.10 한 줄 요약

> 우리는 **바코드 읽는 프로그램**을 만들었고, 그 프로그램이 **거짓말하지
> 않게** 하는 데 가장 공을 들였고, 놀랍게도 **가장 자주 틀린 건 프로그램이
> 아니라 우리가 만든 시험지**였다.

